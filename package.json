{
  "private": true,
  "dependencies": {
    "director": "^1.2.0",
    "todomvc-app-css": "^2.0.0",
    "todomvc-common": "^1.0.1",
    "vue": "^2.1.8"
  },
  "scripts": {
    "build": "vue-cli-service build --target lib --name myLib js/app.js",
    "deploy": "npm run build && git add . && git commit -m \"deploy\"&& git push origin/gh-pages --force ",
    "serve": "vue-cli-service serve",
    "lint": "vue-cli-service lint"
  }
}
